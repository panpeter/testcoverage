name: Release

on:
  push:
    branches: [main]
  pull_request_review:
    types: [submitted]
    branches: ['rel.**']

jobs:
  release:
    runs-on: ubuntu-20.04
    if: github.event.review.state == 'approved'
    steps:
      - run: echo "Works!"
